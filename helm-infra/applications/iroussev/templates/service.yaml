apiVersion: v1
kind: Service
metadata:
  name: {{ include "iroussev.fullname" . }}
  labels:
    {{- include "iroussev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iroussev.selectorLabels" . | nindent 4 }}
