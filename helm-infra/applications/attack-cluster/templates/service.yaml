apiVersion: v1
kind: Service
metadata:
  name: {{ include "attack-cluster.fullname" . }}
  labels:
    {{- include "attack-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "attack-cluster.selectorLabels" . | nindent 4 }}
